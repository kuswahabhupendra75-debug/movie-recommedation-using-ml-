# Dependencies
node_modules/
frontend/node_modules/

# Python
__pycache__/
*.pyc
*.pyo
venv/
env/
.venv/
*.egg-info/

# Build output
dist/
build/
frontend/dist/

# Data files (large)
ml_engine/data/
*.zip

# OS
.DS_Store
Thumbs.db

# ── SECURITY: NEVER commit .env files ───────────────────────────────────────
# These files contain database passwords and API secrets
.env
.env.*
backend/.env
backend/.env.*
frontend/.env.local

# ── Safe to commit (production URL only, no passwords) ─────────────────────
# frontend/.env is committed because it only contains the backend URL
# render.yaml is committed but must NOT have actual passwords in it

# Logs
*.log
npm-debug.log*

